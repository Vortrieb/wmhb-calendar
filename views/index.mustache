<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title>{{title}} - Webmontag Kalender</title>
	<link rel="stylesheet" href="/stylesheets/screen.css" type="text/css" media="screen" title="no title" charset="utf-8">
</head>
<body>
	<h1>Veranstaltungen:</h1>
	<p>
		In Bremen passiert einiges im Netz. Vieles geht leider unter, weil keiner weiß, was wann und wo läuft. Also haben wir diesen kleinen Klender gebaut, der versucht, alle Veranstaltungen zu sammeln.
	</p>


	<h2>Oktober</h2>
	<ul class="event-list">
		{{#dates}}
		<li class="event">
			<div class="event-dropday">22.</div>
			<h3>{{title.$t}}</h3>
			<div class="event-address">
				{{#gd$where}}
					<p><a href="https://www.google.de/maps/preview#!q={{valueString}}">{{valueString}}</a></p>
				{{/gd$where}}
			</div>

			<div class="event-date">
				{{#gd$when}}
				<div class="event-date-start">
					<span class="event-label">Beginn:</span> <time datetime="{{startTime}}">{{#parseDate}}{{startTime}}{{/parseDate}}</time>
				</div>
				{{#endTime}}
				<div class="event-date-end">
					<span class="event-label">Ende:</span> <time datetime="{{startTime}}">{{#parseDate}}{{endTime}}{{/parseDate}}</time>
				</div>
				{{/endTime}}
				{{/gd$when}}
			</div>

			<p class="event-description">{{content.$t}}</p>

		</li>
		{{/dates}}
	</ul>
</body>
</html>
